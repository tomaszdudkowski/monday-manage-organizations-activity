@using Microsoft.AspNetCore.Identity
@model IEnumerable<IdentityRole>

<h1 class="bg-success text-white">ROLE UŻYTKOWNIKÓW</h1>
<a asp-action="Create" class="btn btn-secondary">UTWÓRZ NOWĄ ROLĘ</a>

<table class="table table-sm table-bordered table-bordered">
    <tr><th>ID</th><th>NAZWA ROLI</th><th>UŻYTKOWNICY</th><th>USTAWIENIA</th><th>USUŃ</th></tr>
    @foreach (var role in Model)
    {
        <tr>
            <td>@role.Id</td>
            <td>@role.Name</td>
            <td i-role="@role.Id"></td>
            <td><a class="btn btn-sm btn-primary" asp-action="Update" asp-route-id="@role.Id">Update</a></td>
            <td>
                <form asp-action="Delete" asp-route-id="@role.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-danger">
                        USUŃ
                    </button>
                </form>
            </td>
        </tr>
    }
</table>